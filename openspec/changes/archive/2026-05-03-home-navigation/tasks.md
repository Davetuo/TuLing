## 1. 路由与占位页面

- [x] 1.1 在 `client/src/router/index.ts` 中新增四个功能模块路由：`/chat`、`/spots`、`/trips`、`/albums`，均设置 `meta: { requiresAuth: true }`，组件采用路由懒加载
- [x] 1.2 创建占位页面 `client/src/pages/chat/ChatPage.vue`：展示"智能问答"标题、"功能开发中，敬请期待"提示和返回首页按钮
- [x] 1.3 创建占位页面 `client/src/pages/spots/SpotListPage.vue`：展示"景点探索"标题、开发中提示和返回首页按钮
- [x] 1.4 创建占位页面 `client/src/pages/trips/TripListPage.vue`：展示"行程规划"标题、开发中提示和返回首页按钮
- [x] 1.5 创建占位页面 `client/src/pages/albums/AlbumListPage.vue`：展示"图片纪念墙"标题、开发中提示和返回首页按钮

## 2. 功能入口卡片跳转

- [x] 2.1 在 `HomePage.vue` 的 features 数组中为每个入口添加 `route` 字段（路由 name），导入 `useRouter`
- [x] 2.2 为 `el-card` 添加 `@click` 事件处理，调用 `router.push({ name: item.route })` 实现跳转
- [x] 2.3 为功能卡片添加 hover 样式增强（如轻微上浮 + 阴影加深），提升可点击感知

## 3. 快捷入口区

- [x] 3.1 在 `HomePage.vue` 中新增热门目的地区块：使用静态数据展示 4-6 个热门城市标签（北京、杭州、成都、厦门、西安、三亚），点击跳转至 `/spots?city=xxx`
- [x] 3.2 在 `HomePage.vue` 中新增最近行程入口区块：UI 结构完整，初始展示空状态"暂无行程" + "开始规划"引导按钮（点击跳转 `/trips`），预留 API 调用位置
- [x] 3.3 在 `HomePage.vue` 中新增最近对话入口区块：UI 结构完整，初始展示空状态"暂无对话" + "开始提问"引导按钮（点击跳转 `/chat`），预留 API 调用位置
- [x] 3.4 快捷入口区整体采用异步加载模式（`onMounted` 中加载），不影响功能卡片的即时渲染

## 4. 首页加载状态与骨架屏

- [x] 4.1 实现首页加载骨架屏：使用 `el-skeleton` + 与功能卡片相同的网格布局（`el-row`/`el-col`），展示 5 个卡片骨架占位
- [x] 4.2 实现骨架屏与真实内容的切换逻辑：`onMounted` 中模拟初始化加载，加载完成后切换为真实内容
- [x] 4.3 创建网络状态提示：监听 `window` 的 `offline`/`online` 事件，离线时在页面顶部展示 `el-alert type="warning"` 提示条，恢复时自动隐藏
- [x] 4.4 快捷入口区各区块的局部加载状态：数据请求中展示 `el-skeleton` 行骨架，请求失败展示错误提示，数据为空展示 `el-empty`

## 5. 移动端响应式布局

- [x] 5.1 优化功能卡片移动端布局：确保 `el-col` 响应式断点 `:xs="12" :sm="8"` 生效，移动端两列、平板及以上三列
- [x] 5.2 优化英雄区（hero）移动端：小屏下缩小标题字号至 20px，副标题至 14px
- [x] 5.3 优化快捷入口区移动端：移动端各区块垂直堆叠排列，标签/卡片宽度自适应
- [x] 5.4 调整首页容器最大宽度：从固定 960px 改为 `max-width: 1200px`，适配三列卡片布局
- [x] 5.5 移动端顶部导航优化：屏幕宽度 <= 480px 时隐藏副标题"AI 智能旅行搭子"

## 6. 集成验证

- [x] 6.1 验证已登录用户进入首页 → 5 张功能卡片正常展示 → 点击每张卡片可跳转至对应占位页面 → 从占位页面可返回首页
- [x] 6.2 验证未登录用户直接访问 `/chat`、`/spots`、`/trips`、`/albums` 均被重定向至 `/login`
- [x] 6.3 验证首页在移动端视口（375px、768px）下卡片两列展示、布局不变形
- [x] 6.4 验证骨架屏 → 内容切换流程：首页初始加载展示骨架屏，加载完成后展示真实内容
- [x] 6.5 验证快捷入口区：热门目的地点击携带城市参数跳转，最近行程/对话展示空状态及引导按钮
